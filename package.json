{
  "name": "vincent",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.11.0",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "pnpm --filter @kargain/vincent build && pnpm --filter @kargain/vincent-compiler build && pnpm --filter @kargain/vincent-pipeline build",
    "test": "pnpm build && pnpm -r run test",
    "lint": "eslint .",
    "typecheck": "pnpm --filter @kargain/vincent typecheck && pnpm --filter @kargain/vincent-compiler typecheck && pnpm --filter @kargain/vincent-pipeline typecheck && pnpm --filter @kargain/vincent-publish typecheck",
    "generate:wmi": "pnpm --filter @kargain/vincent-pipeline generate:wmi",
    "generate:seed": "pnpm --filter @kargain/vincent-pipeline generate:seed",
    "validate:seed": "pnpm --filter @kargain/vincent-pipeline validate:seed",
    "validate:full-sim": "pnpm --filter @kargain/vincent-publish validate:full-sim"
  },
  "devDependencies": {
    "@changesets/cli": "catalog:",
    "@eslint/js": "catalog:",
    "@types/node": "catalog:",
    "eslint": "catalog:",
    "typescript": "catalog:",
    "typescript-eslint": "catalog:",
    "vitest": "catalog:"
  }
}
