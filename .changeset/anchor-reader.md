---
"@kargain/vincent": minor
---

Add `@kargain/vincent/anchor` with `createAnchorReader` for reading VincentAnchorRegistry epochs (viem optional peer). Includes protocol-ready bytes32→sha256 conversion and README end-to-end decode example.
