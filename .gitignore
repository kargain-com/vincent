node_modules/
dist/
coverage/
pipeline/.cache/
pipeline/.build/
contracts/cache/
contracts/artifacts/
contracts/coverage/

# Environment (never commit secrets)
.env
.env.*
!.env.example

# Local maintainer notes (never committed / never published)
*.local.md
docs/HANDOFF.md
docs/SESSION.md
publish/docs/MAINNET_READINESS.md

# Editor / agent tooling (local only — never commit)
.cursor/

# Publish resume checkpoint (local state only)
publish/.vincent-publish-checkpoint.json
publish/.vincent-publish-checkpoint.json.tmp
publish/.vincent-community-checkpoint.json
publish/.vincent-community-checkpoint.json.tmp
publish/leaf-uris-epoch-*.json
