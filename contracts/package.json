{
  "name": "@kargain/vincent-contracts",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "license": "MIT",
  "scripts": {
    "build": "hardhat compile",
    "test": "hardhat test",
    "compute-address": "hardhat compile && hardhat run scripts/compute-address.ts",
    "deploy:base-sepolia": "hardhat compile && hardhat run scripts/deploy-create2.ts --network baseSepolia",
    "verify:base-sepolia": "hardhat run scripts/verify.ts --network baseSepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox-viem": "^5.0.7",
    "@nomicfoundation/hardhat-verify": "^3.0.21",
    "@types/node": "catalog:",
    "dotenv": "^16.4.7",
    "hardhat": "^3.9.1",
    "typescript": "catalog:",
    "viem": "^2.31.0"
  }
}
